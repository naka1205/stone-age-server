# deviations / 04-doc-vs-source — 我方文档与原版源码的分叉实证

> ★ 本目录 = **和原版哪里不一样**。流程账看 [`../journal/`](../journal/) ·
> 架构裁定看 [`../00-architecture.md`](../00-architecture.md) ·
> 决策看 [`../11-decision-register.md`](../11-decision-register.md) · 还欠什么看 [`../backlog/`](../backlog/)。

> ⚠️★★ **本文件是派生索引,不是真源。** 每条只给一句话 + 指回 journal / `11` 的锚点。
> 若本文件与 journal 或 `11` 冲突,**以那边为准**。
>
> ⚠️★★ **本文件与其余三份性质不同**:那三份是「**我方实现** vs 原版」,
> 本文件是「**我方文档**(17 份取证文档 + `stoneage-plan/`)vs **原版源码**」。
> ⇒ 它记的不是交付物属性,是**取证教训** —— 但它值得单列,因为**每一条都曾差点变成实现偏差**。

---

## 纪律 ①(本文件存在的理由)

> **★★ 动手前回原版源码核实判定阈,不凭 17 份文档转述。**

★★★ **一致不等于对。** 已 **≥7 次**抓到文档与源码分叉,其中**多次是四份文档一致地写错**。
⇒ 「N 份文档都这么说」**不构成证据**,它们往往同源。

⚠️ 连带:「缺判定阈 ⇒ 永久留空」这种结论**本身要回源码复核** ——
「**源码真缺阈才留空,文档没抄不算**」([§9.0.21](../journal/10-battle-core.md) 立)。

---

## 1. 公式 / 判定阈类

| # | 文档写的 | 源码实际 | 若照文档实现 | 出处 |
|---|---|---|---|---|
| 1 | 三分段有窄缝 | **无窄缝** | 边界值错 | 早期批次 |
| 2 | 首次 `escape_cnt = 1` | **双重计数**(调用点 `+2`) | 首次逃跑概率错 | [§9.0.19](../journal/10-battle-core.md) |
| 3 | 捕获级差是**整数除法**(`05` §6.2 原文标注) | **浮点除法**(`At_Level` 等全声明为 `float`) | 捕获率整体偏 | `11` DR-BT16 |
| 4 | 暴击 / 反击「**没给判定阈**」 | ★★ **源码里齐全**(`AttackSeq:1592` 有 `RAND(1,10000) < perCri`)—— **文档没抄** | 整个暴击**被判为不可实现而留空** | `11` DR-BT17 · [§9.0.21](../journal/10-battle-core.md) |
| 5 | ★★ **四份文档一致地**写「+ 等级」 | 公式里**根本没有 `level`** | 属性推导整体偏 | [§9.0.29](../journal/11-model-attr.md) |

## 2. `05` §4 状态系统的**六处偏差**(重灾区,L4.1 开工取证一次抓到)

★ 多数**返回值断言抓不到**,所以每条都要问「按文档实现会怎样」:

| # | 文档写的 | 源码实际(8.0 树) | 按文档实现的后果 |
|---|---|---|---|
| ① | §4.1「职业路径**没有**互斥循环」 | `:5195` **有**;只是三属抗 + 冰爆术 1-10 在循环**之前** `return 1` 绕过 | **所有**职技都能叠加状态,而原版只有 **11 种**能 |
| ② | §4.3 麻痹「− 抗性 − **装备抗性**」 | `:5081-5083` **只减** `RegTbl[status]` | 麻痹命中率凭空变低 |
| ③ | §4.3「`max(per, 0)`」 | 源码**没有** | 无可观察后果 ⇒ 冗余(见 [`02-faithful-quirks.md`](02-faithful-quirks.md)) |
| ④ | §4.3 通用分支「− 装备抗性」 | `:5141-5151` **只对虚弱 / 魔障 / 沉默**三种,分属 `_EQUIT_RESIST` + `_SUIT_ADDPART3` | 其余 **39 种**命中率全偏 |
| ⑤ | §4.3「命中率硬上限 80%」 | 在 `else` 内 ⇒ **麻痹不夹** | 无可观察后果(`per` 恒 20)⇒ 冗余 |
| ⑥ | §4.4 酒醉解除「`QUICK *= 2`」 | `:5490` **有 ridepet 分支** | 有骑宠时敏捷幅度错 |

★ **另一面值得同样记下**:四张表的长度 `05` §4.5 **全对**
(`StatusTbl` 44 / `RegTbl` **31** / `aszStatus` 32 / `aszStatusFull` 31)——
难得一次文档逐项吻合,**记下来免得下次连对的也去重核**。 → [§9.0.55](../journal/15-status.md)

## 3. ★★★ 排期依赖图错(最贵的一类)

> **「反击排在 L4 状态系统之后」—— 这个四份文档一致的前提,有一半不成立。**

- `BATTLE_GetDamageReact`(`battle_event.c:1819`)读的是 **5 个独立 work 字段**
  (`WORKDAMAGEVANISH` / `ABSROB` / `REFLEC` / `WORKTRAP` / `WORKACUPUNCTURE`),
  **一个都不在 `StatusTbl` 的 44 项里**、不受 §4.1 全局互斥约束;
- `BATTLE_CounterCheckPlayer`(`:3465`)更是**一个 status 槽都不读**。

⇒ 那 5 个字段的写入者是宠技 / 职技 / 魔法(均未移植)⇒ 净核里恒 0 ⇒ `react` 恒 `NONE`
⇒ **反击可在 `react == NONE` 路径上独立落地,不必等状态系统**。

★ 与第 1 节第 5 条同族,**但这次错的是排期依赖图不是公式** —— 它影响的是**做什么的顺序**。
→ [§9.0.55](../journal/15-status.md) · `11` DR-DT24

## 4. 定位错(找错函数 / 找错文件)

| 项 | 错的定位 | 真相 | 出处 |
|---|---|---|---|
| **野怪掉落** | `NPC_NPCEnemy_Dying` | 那是**明雷专用**(依赖 D6);野怪走**另一套三阶段** `BATTLE_AddExpItem` | [§9.0.51](../journal/14-item.md) |
| ⚠️★★ **敌人表文件** | `enemy.txt`(33 列) | **`enemy1.txt`(34 列)** —— 前者是宏关闭时代的旧数据 | [§9.0.35](../journal/12-encounter.md) |
| **明雷退回** | DR 记「已实现,W.5 改成开战」 | **从未实现** —— `walkStep` 只判地形、世界敌人不占格 ⇒ W.5 是**从零做两者** | [§9.0.46](../journal/13-world.md) |
| **`CHAR_PETID` 匹配键** | 记作「归 D 线不建」 | 值在**已导入的模板行**里(`= *(tp+E_T_TEMPNO)`) | [§9.0.50](../journal/14-item.md) |

⚠️★★ **`enemy.txt` 那条最险**:两文件 int 列**整体错位一位而每列都还是合法整数**
⇒ 按错文件量出的分布**看起来完全合理**(据此得出「min>max 占 60%」,换对文件后是 **0%**)。

## 5. 名字会骗人

| 项 | 说明 | 出处 |
|---|---|---|
| **形参名** | `ENEMY_RandomChange(_, tempno)` 与 `ENEMY_RandomEnemyArray(e_array, _)` 的**实参都是 `ENEMY_ID`** | [§9.0.35](../journal/12-encounter.md) |
| **`fmdplevelexp`** | ⚠️ 是**家族声望**,不是升级经验(升级走 `exp.txt`) | [§9.0.40](../journal/12-encounter.md) |
| **同名不同内容** | 数据基准是 `csa8.0/gmsv/data/`,**不是** `StoneAge/gmsv/data/` | — |

## 6. ⚠️ 核对对象也可以是「上一次会话自己的实现与注释」

★★ **纪律 ⓪ 的新变体**([§9.0.53](../journal/14-item.md) 立):

> 病灶是「**只跟到取到值的那一行就下结论,少跟了一层调用**」,
> 并且给结论**补了一个引着准确行号的理由**(`battle_item.c:289` 确实是 `sscanf`)。
> ⇒ **编造的是「不必照抄」的理由,比编造「它很要紧」更难发现 —— 因为它省掉代码而不是增加代码。**

⇒ **处置:接手未提交的工作树前,先把它的关键断言(尤其「不摇 rng / 不需复刻」这类减法结论)回源码跟到底。**

---

## 回写纪律

⚠️ `stoneage-plan/` 是取证工作流的**入口线索** —— **错误留在入口会被反复读到**。
⇒ 凡在此抓到的分叉,按 `stoneage-plan` §9.1 回写纪律**同步勘误那边**。

---

## 另见

- **有意改变(架构性)** → [`01-intentional-changes.md`](01-intentional-changes.md)
- **照抄的原版缺陷与冗余** → [`02-faithful-quirks.md`](02-faithful-quirks.md)
- **净核划外 / 未移植** → [`03-not-ported.md`](03-not-ported.md)
