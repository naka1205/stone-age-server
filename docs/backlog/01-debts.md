# backlog / 01-debts — 已知欠债表

> ★ 本目录 = **还欠什么**(遗留 / 暂缓 / 未决 / 做不了)。
> 流程账看 [`../journal/`](../journal/) · 架构裁定看 [`../00-architecture.md`](../00-architecture.md) ·
> 决策看 [`../11-decision-register.md`](../11-decision-register.md) · 与原版差异看 [`../deviations/`](../deviations/)。

> **来源**:原 `01-server-architecture.md` §13,**逐字迁入,编号与顺序照原样未动**
> (⚠️ 原表编号有两处乱序:22 在 21 之前、25 在 24 之前 —— 保持原状,不重排,
> 因为编号被 journal 与 `11` 的多处记录引用)。
>
> **保留 33 条历史编号**。2026-09-12 修复更新见条目 22 及 32/33 补记；原阶段数量不作为本轮验收证明。
>
> ⚠️ **已闭合的条目(✅ 删除线)不删** —— 它们记着「这条债当初是怎么关掉的」,
> 而那正是同类债再次出现时最有用的东西。

---

## 13. 已知欠债

| # | 欠债 | 说明 |
|---|---|---|
| 1 | 🔄 ★ **`BATTLE_Battling` 移植为 L3 —— 批次 0 / 0.5 / A.1 / A.2 / A.3 / A.4 已落地,其余未做** | 阶段 **0.1/1.1**(★ 0.1 与 1.1 已于 2026-08-31 合并 —— `00` §9.0.3 裁定**一遍到位,不做中间态**:两段式产出的"改造后的 C"因 P1/P2 永远无法编译测试,且 720 处要编辑两次)。**已落地**:批次 0 的四条公式 + 黄金用例集;★ **批次 0.5 的调度骨架**(`ResolveTurn`:行动顺序含 DR-BT8 · 指令分发 · 攻击次数 · 防御减伤六档 · 骑宠分摊 DR-BT2 · 事件产出与截断,2026-09-03);★ **批次 A.1 逃跑**(`RollEscape`,DR-BT15,2026-09-06,`00` §9.0.19);★ **批次 A.2 捕获**(`RollCapture`,DR-BT16,`00` §9.0.20);★ **批次 A.3 暴击**(`RollCritical` / `ComputeCriticalDamage`,DR-BT17,`00` §9.0.21);★ **批次 A.4 打飞 / 究极一击**(`RollKnockback` + `KnockbackState` 回写事件,DR-BT18,`00` §9.0.22)。用例集 33 → **72 用例 / 2,394 断言**(2026-09-07 实测复核)。**未做**:批次 A 余项(道具/宠技/职技/咒术)/ B / C / D · **换宠移到 L2 之后** · **反击排在 L4 之后** · **打飞下游后果**(回合内目标排除 / 阵亡额外战果,`00` §9.0.22 ③)。⚠️ 规模按 `00` §1.3.1 批次表(第②步净规模 **720** 处,不是 1,047 —— 31.2% 是指令登记)<br>✅★ **子项(2026-09-03 发现,2026-09-06 A.3 澄清 + 部分关闭)**:`05` §3.3 暴击与 §3.5 反击当时被判「只给 `per` 构成式、没给判定阈 ⇒ 留空」。**回源码核实后,那前提对暴击不成立**(DR-BT17):判定阈在 `AttackSeq:1592` 齐全,是**文档没抄、非源码没有** ⇒ 暴击已在 A.3 实现。**反击的判定阈也齐全**(`CounterCheckPlayer:3506`,行列归属与 `At_Luck` 归攻方都清楚)⇒ 当年"含糊"的判断也是文档没抄;**它仍未做的真实理由是依赖 `GetDamageReact`(L4 状态系统)**,排在 L4 之后。★ 教训:「文档没给判定阈」≠「原版没有」,须回源码复核再决定留空/实现。 |
| 2 | ✅ ~~**D2 的可撤销点未兑现**~~ | ★★ **已于 2026-08-31 兑现并关闭**(阶段 0.0):全闭包 + 全指令分支 + 两张技能表重跑,覆盖 340 个函数(13.1×)⇒ **D2 存续、D1 不动**。见 `00` §1.1;产物 `stoneage-plan/tools/battle_purity_full.json` |
| 3 | ✅ ~~协议 IDL schema 未定稿~~ 选型已定,**schema 本身仍待写** | DR-TS1 已裁定(`11` §1.1):`.proto` 语法 + 自写 codegen → 纯 POD。阶段 0.2 可开工 |
| 4 | 6 项**永久不可判定** | `00` §10.2;其中 `fmdplevelexp` 是 **10 倍的裁定风险敞口,确认无法关闭** |
| 5 | 实现期待补 **69 项 / ≈75–110 天** | 已随 D8 裁定同比例下调;**不是可选项** |
| 6 | 两项待服务器实测 | X Plugin(33060)是否启用;MySQL 是否已配 TLS |
| **7** | ✅ ~~★ **D2 的两端编译:标准库与浮点已出清,MSVC 方言待一次性收口**~~ ⇒ **已于 2026-09-02 结案** | 2026-09-02 客户端仓完成首次实证(`00` §9.0.5);同日 **GCC 15.2 / libstdc++** 四组合全绿出清 A 类(标准库差异)与 C 类(浮点逐位一致,`-ffp-contract=off` 固化)(`00` §9.0.6);**当日晚 B 类 MSVC 方言由 `tools/win_validate.ps1` 一次性收口(`00` §9.0.7.1):11 项全绿,两仓 MSVC 构建各 0 告警,客户端黄金用例集 1/1(2,148 断言),★ 断言防线反向验证通过**(改错相克矩阵后 `assert` 确实触发 ⇒ `/UNDEBUG` 的 MSVC 分支有效)。⇒ ★★ **D2「一份规则两端编译」在桌面三套工具链(Apple clang / GCC / MSVC)结案。**<br>⚠️ **未结案的两项须留着**:① **emcc(wasm)/ Android NDK** —— 分别绑 D4 解冻与移动端出包,不在当前路径;② ✅ ~~★ **CI 的 `windows-latest` 仍要挂**~~ ⇒ **2026-09-03 已挂**(`00` §9.0.9):GitHub Actions 三平台矩阵(ubuntu/GCC · macos/clang · windows/MSVC),判定逻辑在 `tools/ci_verify.py`。✅ **同日首跑三平台全绿、五项全过 ⇒ ② 已真正兑现**(`00` §9.0.10;欠债 10 同步关闭) |
| **8** | 🔧 ★ ~~**`shared/` 的锁定 ref 尚未推到远端**~~ ⇒ **已不成立,剩「打正式 tag」** | ⚠️ 原文写「客户端发布态(FetchContent)此刻取不到」—— **2026-09-02 复核:两仓均已与 `origin/master` 同步,`775794a` 实测是 `origin/master` 的祖先** ⇒ **发布态现在取得到**,该阻塞已消失。⇒ 真正剩下的只有:把锁定 ref 从裸 SHA 换成**正式 tag**(两仓 `git tag -l` 均为空)。★ 连带解开客户端 `01` §12.3 记的「CI `windows-latest` 的前置是欠债 9」—— **前置已具备**,CI 可以挂了 |
| **9** | ⏭ ★ **`idl_verify`(schema 与生成物同步)依赖 `protoc`,而 `protoc` 不在每台机器上** | 2026-09-02 Windows 验证时暴露。`protoc` 是**构建期工具**(DR-TS1 边界 ①:运行时不链接 libprotobuf;DR-TS2:生成物入库)⇒ **只消费生成物的人不需要装它**,「本机没有」是正常状态。已处置:`sgidl_gen.py` 给清晰诊断 + 支持 `PROTOC=<路径>`;`tests/CMakeLists.txt` 按「Python3 缺失」的先例**不注册测试 + `message(WARNING)`**(常红会训练人忽略红色,`00` §10.4)。⚠️ **但 CI 上必须有 `protoc`,否则这道关会静默消失** —— 那比常红更坏。⇒ ✅ **2026-09-03 处置**:三个平台的 workflow 各装一次 protoc,★★ **但真正接住这件事的不是安装步骤,而是 `tools/ci_verify.py` 的测试注册清单断言** —— 装了 protoc 不等于 `find_program` 找得到、不等于测试注册上了,只有「注册的测试集合 == 期望集合」这条断言会在它消失时**失败**。⇒ 本条降级为「已有守卫,保留说明」 |
| **10** | ✅ ~~⚠️★ **新增(2026-09-03):CI 的 workflow 本身尚未在 GitHub 上真跑过一次**~~ ⇒ **当日归因完毕并关闭** | **原文**:`tools/ci_verify.py` 已在本地 macOS/clang 下五项全绿(含测试注册清单、清洁构建 `SG_WERROR`、断言防线反向验证),客户端侧的发布态也在临时目录里真跑通了。**但 `.github/workflows/ci.yml` 本身 —— 矩阵展开、runner 镜像、三条 protoc 安装步骤、Windows 侧的 VS 生成器选择 —— 全部未经执行。**⚠️ 首跑很可能红,且**红的原因多半与本仓代码无关**(runner 的 GCC 版本比本地的 15.2 旧 ⇒ `SG_WERROR` 下冒出新告警;`choco install protoc` 的包名/可用性;`seanmiddleditch/gha-setup-ninja` 的版本)。★ 这与 `00` §9.0.7.1 的教训同族:**「脚本写好了」和「脚本真跑过一次」之间隔着五个缺口** —— 那次一次性验证的全部价值,恰恰在首跑没通过以及为什么。⇒ 首跑结果须逐条归因后再写文档,**不要在跑通之前把欠债 7 的 ② 当成已兑现**。<br>✅ **首跑结果(`00` §9.0.10)**:★ **预判的三种红一个都没发生** —— 服务端 `verify` **三平台全绿、五项全过**(配置 `SG_WERROR=ON` · 测试注册清单 5/5 · 清洁构建 0 告警 · `ctest` 5/5 · 断言防线反向验证)⇒ **欠债 7 的 ② 一并兑现**。★ 附带一笔此前未知的凭据:三个 runner 自带的 protoc 分别是 **3.21.12 / 35.1 / 36.1**,跨一个大版本时代而 `idl_verify` 仍过 ⇒ **生成物不依赖特定 protoc 版本**(欠债 9 只处置了「本机没有」,从未回答「版本差异要紧吗」)。<br>⚠️★ **真正的红在预判之外,且在客户端侧**:三平台同一步同因 `could not read Username for 'https://github.com'` ⇒ 两个镜像仓当时是 private,而 runner 的 `GITHUB_TOKEN` 只授权它自己那一个仓 —— ★★ **这是 `00` §9.0.9「tag 必须同时存在于两个远端」那条耦合的第二面(跨仓可见性),17 份文档里没有一处记过**,而 tag 一致性当时恰恰是满足的。⇒ 已按用户裁定把两个镜像仓改为 **public**,重跑三平台全绿并**取日志逐项核验**(发布态 · 锁定 ref `shared-v0.1.0` · 33 用例 / 2,276 断言),不凭绿灯结案。⚠️ **遗留约束**:public 是客户端 CI 成立的前提,**改回 private 必须同时落地 PAT secret 方案**,否则会回到那个与"权限设置"看不出关系的红 |
| **11** | ✅ ~~⬜ ★★ **命名前缀 `sg_` → `sa_` 尚未执行**(新增 2026-09-04)~~ ⇒ **当日执行完毕并关闭** | **原文**:`00` §9.0.11 已裁定,**代码未动**。⚠️ 三处「改一半就红」的耦合须同批完成:① 跨仓契约 `SG_SHARED_GIT_TAG` / `SG_SHARED_SOURCE_DIR` / `SG_SHARED_GOLDEN_TEST`(两仓 + 重打 tag);② 两仓 `ci_verify.py` 的 `EXPECTED_TESTS` 是**目标名字面量** —— ★ 它会失败,而**那正是它该有的行为**,不要为改名放宽;③ `check_shared_purity.py` / `win_validate.ps1` 里**按名字匹配**的检查 —— 漏改是**静默失效**,比失败更坏(`00` §10.4)。⇒ 排在批次 A 之前<br>✅ **执行结果(2026-09-04:`ba2908f` 文档 · `d7a5754` 代码 · `d810c30` 归因;客户端 `7b10d25` / `06ff952`)**:全仓改名落地,`shared-v0.2.0` 已重打并前推客户端锁定 ref,两仓 × 两远端已推,**CI 三平台全绿**。⚠️ 预警的三条耦合**都按预期发生并按预期处置**(② 确实失败了,没有为改名放宽它)。<br>⚠️★ **但真正值得记的是预警自己漏掉的那一处**(`00` §9.0.12):`ci_verify.py` 仍传 `-DSG_WERROR=ON` 而 option 已成 `SA_WERROR` ⇒ CMake 只印一句 unused-cli、配置照样成功,**`-Werror` 自 `d7a5754` 起一直没开过**,而报告里那句「`SA_WERROR=ON`」是硬编码文本、**从来不是观测**。⇒ 本条预警点名了 ② 与 ③,**漏的恰恰是同一个文件里的另一行** —— 「按名字匹配的地方要同批改」这条纪律,连写下它的人也没能穷举完自己点名的文件。已加防线:配置期出现 `Manually-specified variables were not used` **即硬失败并列出变量名**(原则:凡靠某个 `-D` 才成立的结论,先证明那个 `-D` 被项目收下了),防线本身做了反向验证 |
| **12** | ✅ ~~⬜ ★ **`TcpTransport` 未做 —— 1.5 的收尾项**(新增 2026-09-04)~~ ⇒ **2026-09-05 关闭** | **原文**:`src/net` 目前的传输实现是 `LoopbackTransport`(`net/api.h` 卷首已注明**是有意切分不是遗漏**,它同时是单容器形态下 `InProcTransport` 的雏形)。⇒ 成帧 · 会话 · 编解码接入都已在 Loopback 上验过 ⇒ 补 TCP **不动上层**。⚠️ 它是 1.4 端到端 demo 的前置<br>✅ **落地**(`00` §9.0.14):★ 原生 socket + `poll(2)`,**不走 asio**(DR-TS8,用户裁定)· 13 条真 socket 用例(★ 出站背压与续写 · 优雅关闭 · 端到端握手)· `main.cpp` 接线(绑端口失败拒绝启动 · SIGINT/SIGTERM 停机 · ★ 入口首次有测试 `server_self_test`)。★ **「上层一行没动」如期兑现** —— 印证了当初切分的理由。⚠️ 一条用例修过偶发红(内核自动调大缓冲把 3 MB 全吃下 ⇒ 客户端显式设小 `SO_RCVBUF` + 负载递增),30/30 绿 |
| **13** | ⬜ ★ **`poll(2)` 是 O(连接数)**(新增 2026-09-05) | DR-TS8 认下的代价。上规模时换 epoll / kqueue / IOCP,`ITransport` 不动、上层不动。⚠️ 何时算「上规模」由 `15` 的容量画像决定:单线路个位数千连接内,poll 一次系统调用传整个数组不是主项(原版是每连接一次 `select`,§5.1)。属阶段 3 |
| **14** | ⬜ **节拍源是主线程 `sleep_for`,不是 §2 的定时线程**(新增 2026-09-05) | `main.cpp` 的 tick 循环按**绝对期限**睡到下一拍(不把 Tick 耗时累积成漂移)—— 1.5 的最简形态。§2 的「定时线程节拍源」与 I/O 线程池一起属阶段 2;届时 tick 第 1 步「统一时钟源」不变,变的只是谁在唤醒主线程 |
| **15** | ✅ ~~🔧 ★★ **`shared/wire/` 已落地,但锁定 ref 未前推**(新增 2026-09-06)~~ ⇒ **当日关闭** | `00` §9.0.15。`shared/` 这次**真的变了**(新增子树 + `CMakeLists` 改动)⇒ 按 §9.0.9 的耦合纪律必须打 **`shared-v0.3.0`** 并前推客户端 `SaShared.cmake` 的 `SA_SHARED_GIT_TAG`。⚠️ **在那之前客户端发布态(FetchContent / CI 的 d2-gate)编译的仍是没有 `wire/` 的那一份** —— 本地 `d2-only` 走联调态、已验过,**它绿不能替发布态作证**(与 §12.8 ① 同一条)。★ 两仓 × 两远端一起推,tag 必须同时存在于 gitee 与 GitHub。<br>✅ **已完成(2026-09-06)**:`shared-v0.3.0`(= `04d406c`)在 gitee 与 GitHub 均已存在,客户端 `SaShared.cmake` 的 `SA_SHARED_GIT_TAG` 已锁到它(`c0939a7`),两仓工作树干净且与远端同步 ⇒ **发布态取到的那一份现在含 `wire/`**。|
| **16** | ✅ ~~⬜ ★★ **1.4 只完成了服务端这一半**(新增 2026-09-06)~~ ⇒ **当日关闭:客户端侧已落地** | `00` §9.0.16。服务端侧:会话就绪即入场 · 入场下发 `BattleSelfInfo` + `BattleTurnBegin` · 真二进制 × 真 TCP 已跑通。~~**客户端侧 `src/net` / `src/battle` / `BattleScene` 仍未开工**~~。⚠️★ 测试里的 `ClientMirror` 证明的是**服务端这一侧的线上契约自洽**(帧能对齐 · 信封能解 · 字段有值 · 上行能被采纳),**不是客户端能跑** —— 别把它读成 1.4 已完成。★ 但它确实缩小了客户端的验证面:客户端用的是同一份 `shared/wire`(DR-TS9 乙案)⇒ 那三样不必再验一遍,它要验的是**表现层**。<br>✅ **客户端侧落地(2026-09-06,客户端 `01` §12.12)**:`sa_net`(拨出式 `TcpTransport`)· `sa_battle`(`BattleEvents` 逐条消费)· `BattleScene`(占位美术 + 每回合自动出招);★ 前两层**零引擎依赖** ⇒ 协议层用例 `client_net`(**7 用例 / 263 断言**)可进 CI。★★ **端到端已跑通**:服务端 `--config config/demo.json` + 客户端 `--battle --autotest` ⇒ `joined=true turns=2 event_msgs=2 events=4 PASSED`,服务端日志 `battle_joined` → `battle_turn_resolved ×2` → `battle_finished turns=2` ⇒ **1.4 完整,阶段 1 的验收点达成**。<br>⚠️★ 客户端侧当场抓到三个「只有真跑才会暴露、且不报任何错」的缺口(拨号早于配置赋值 · 握手在帧循环里抢跑被静默丢包 · `scheduleUpdate()` 漏调),另修一个**既有**缺陷:客户端 `BootScene.cpp` 的 `SG_LOG_*` 被 DR-TS7 改名误改成 `SA_LOG_*`(**那两个宏属于引擎,不属于本项目**)⇒ 自改名起就编不过而两天无人发现,因为 CI 跑 `d2-only` 不建 core 与入口。★ 详见客户端 `01` §12.12 ②<br>⚠️ **新增连带欠债(客户端侧欠债 16)**:端到端那条证据**不在 CI 上且短期不会在**(需要跑着的服务端进程)⇒ 装配层每次改动须本地手工复跑 |
| **17** | ⬜ ★ **`demo_battle` 是必须被删掉的脚手架**(新增 2026-09-06) | `00` §9.0.16 ②。它让「握手完进哪里」这个问题有了一个**临时且错误**的答案 —— 真答案是**选角 + 登录点**(阶段 2,要 storage)。⚠️ 默认关 + `config/README.md` 写明了删除时机,但**没有任何东西会在阶段 2 提醒你删** ⇒ 记在这里。涉及:`ServerConfig::demo_battle` · `config.cpp` 的解析段 · `World::OnSessionReady` 里那一段 · `MakeDemoField()` · `config/demo.json`。★ 反过来,`JoinBattle` 里下发 `BattleSelfInfo` + `BattleTurnBegin` 那段**要留着** —— 它不是 demo 专用的 |
| **18** | ⬜ **`BattleSelfInfo.menu_flags` 恒为 0**(新增 2026-09-06) | 菜单构成(观战加入 / 先制 / 宠物菜单开关,`MenuFlag` 7 个取值)属阶段 2 的 UI 契约 ⇒ 1.4 **不猜**,与批次 0.5 对暴击 / 反击的处置同一条纪律。⚠️ 客户端若据它置灰菜单,现在会得到「什么都没开」——⇒ 客户端 1.4 **不应**消费这个字段。★ 同一条消息里的 `cannot_act` 相反:它照 `rules::CheckCanAct` 填,**可以消费** |
| **19** | ✅ ~~🔧 ★★ **本批(§9.0.17)的两项收尾:MSVC 未验 + 锁定 ref 未前推**~~ ⇒ **当日两项全部兑现并关闭**(2026-09-06) | `00` §9.0.17 ⑧。① **只在 GCC 15.2 与 Apple clang 上跑过** —— 与 §9.0.16 那条同纪律、方向相反(那次缺 GCC/MSVC,这次只缺 MSVC)。⚠️ 而本批改的正是**生成物的形状**,MSVC 是唯一还没看过新形状的编译器。<br>② ★★ **必须前推锁定 ref 到 `shared-v0.4.0`**,且这是那条耦合纪律的**第三面**:前两次前推的理由是 `shared/` 的**内容**变了(v0.2.0 改名)与**形状**变了(v0.3.0 新增 `wire/` 子树),⚠️ **本次 `shared/` 一个字没动 —— 变的是它依赖的兄弟目录 `idl/generated/cpp`**(`shared/CMakeLists.txt` 的 `sa_idl` INTERFACE 指向 `../idl/generated/cpp`)。⇒ 客户端发布态取到的那一份仍是**带早退的旧生成物**,而本地 `d2-only` 走联调态看的是工作树 —— **联调态绿不能替发布态作证**(与 §9.0.15 同一条)。★ 那道漂移守卫**接住了它**:配置期精确列出 7 个改动文件并写明「拿它下『D2 通过了』这个结论之前,先提交」⇒ 说明 `SA_SHARED_WATCHED_PATHS` 当初把 `idl/generated/cpp` 列进去是对的;⚠️ 但 17 份文档**从未记过「`shared/` 没变也可能要前推」** —— 这一面直到本批才出现<br>✅ **兑现(2026-09-06 当日)**:① 服务端 `fcc8f01` + tag `shared-v0.4.0` 推两远端(tag 两端一致 `80a88ae → fcc8f01`),CI **三平台全绿**并取日志逐项核验 —— ★ **GCC 侧 `maybe-uninitialized` 命中 0 次**(原红彻底消失)· ★★ **MSVC 侧 `idl_smoke` Passed**(新生成物形状在 MSVC 上成立、四条新用例通过)· 三平台 `ctest` 各 11/11 · 六项各全过。⚠️ 核验时先排除了一处误读:`ctest -N` 会对**全部** 11 个测试打印 `Could not find executable` —— 它跑在构建**之前**,那时可执行文件本就不存在,不是"测试没跑"。<br>② 客户端 `211d341` 推两远端,`d2-gate` **三平台各八项全过**:接入模式 `fetch`(发布态)· 锁定 ref `shared-v0.4.0` · ★ 锁定 ref 与源码一致 · **33 用例 / 2,276 断言** · 清洁构建 0 告警 · `ctest` 2/2 ⇒ **发布态取到的那一份现在含本次修复**。★ 顺带坐实 §9.0.10 那处「约束记宽了」的复核:实测 `stone-age-server` = public 而 **`stone-age-client` = private**,`d2-gate` 照样全过 |
| **20** | 🔧 ★★ **L2 实体池已落地但未接线到 `World`** ⇒ **2026-09-07 批次 M.1 部分关闭** | `00` §9.0.24 ④ / §9.0.26。**原文**:`shared/model/` 的 `EntityPool` / `EntityKind` / `EntityIndex` 是**纯结构地基**,`ModelPoolTest` 11 例钉住了池与索引本身的行为,但**没有任何一个实例挂在 `World` 上** ⇒ `applyEvents` 里捕获的「生成宠物入队」、A.4 的 `pet_hp_delta` 落地、换宠仍是注释。⚠️ **这条不能靠"迟早会接"消掉**:地基绿而运行时不接,`ctest` 一样 12/12 全过,和 §9.0.16 那条「`OnSessionReady` 只打日志」是同一族静默。★ 接线时的三笔:① 各族池实例化 · ② 脏槽跟踪 / 活跃集 · ③ 捕获与打飞的世界写落脚点<br>✅ **批次 M.1 已兑现(2026-09-07,`00` §9.0.26)**:① `EntityPool<Player,100>` + `EntityPool<Pet,2000>` + `ConnIndex` 挂上 `World::Impl`,会话就绪即建 Player、断线即释放(★ **连它的宠物一并回池** —— 漏了这半边会让 Pet 池只增不减,而没有任何一处报错)· ③ **捕获的世界写照源码七步落地**(capture_bonus 无条件清零 · 生成宠物是**门** · 挂主人槽 · 捕获计数 · 目标离场),`pet_hp_delta` 落到 `ride_hp`。用例 `model_pool` 11/59 → **20/96** · `world_tick` 18/223 → **25/300**,反向验证三处。<br>⬜ **仍未做的两项**:② **脏槽跟踪 / 活跃集** —— `EntityPool.h` 文末已写明要与 tick 调度对齐,而 tick 的 `kCharLoop` **已于 W.1 实装**(`00` §9.0.42,玩家段)⇒ 脏槽 / 活跃集现在可与它对齐,建议**独立小批**(不塞进 W.1 免过大);★ **各族字段的完整面** —— 本批只建了捕获链路要读写的那一小块(`03` §11 欠债 1 的 693 字段逐条含义、§4 的 65 条别名展开都没动)。<br>⚠️★ **本批推翻了本条原文里的一处前提**:「A.4 的 `pet_hp_delta` 落地」**从来不需要 L2 池** —— `Combatant::ride_hp` 自 `b4670d8`(阶段 1.0)就存在,比写下那条注释早三天(`00` §9.0.26 ①)。⇒ 这条欠债当初把一件能做的事一并记成了被阻塞<br>✅ **批次 M.2 续(2026-09-07,`00` §9.0.27)**:「战场态宠物入场」机制落地 —— `enterPetToField` / `exitPetFromField`(`src/world`,把 `Model::Pet` 投影成 `slots[主人+5]` 的 `Combatant`,三道门 + 两源码陷阱改判),是换宠指令的地基。⚠️ 但投影出的**战斗三围恒 0**,见新增欠债 23。 |
| **22** | ✅ **捕获/换宠的提交结果与下行消息一致**（2026-09-12，F04/F08/F12） | World 逐行动提交后才发布结果：捕获资源失败将 flags 改为 0，换宠失败不发布入场；成功后发送占位和 HP 快照。槽满、空宠槽的真实编码帧已有回归。共享规则只产意图，不读取实体池。原问题及曾认为 D2 阻塞修复的历史见 [§9.0.26](../journal/11-model-attr.md) / DR-BT21；当前验收见[修复结果](../audits/2026-09-12-remediation-results.md)。 |
| **21** | 🔧 ★ **`shared-v0.8.0` 曾漏推远端 —— 已于 2026-09-07 以 `shared-v0.9.0` 结清**(新增并当日关闭) | `00` §9.0.22 ② 写明 A.4 后「锁定 ref 须前推 `shared-v0.8.0`,两仓 × 两远端一起推 tag」,⚠️ **该 tag 只建在本地,两个远端都没有**(2026-09-07 `git ls-remote` 实测:origin 与 github 均只到 `shared-v0.7.0`)⇒ 客户端 `2e1200d` 把 pin 指向它的那段时间里,**发布态在任何平台都取不到该 ref**,不只是「Linux/MSVC 解析失败」。★ 这一条与 §9.0.23 ① 的归因有出入,归因修正见 `00` §9.0.25 ②。✅ **处置**:本轮打 `shared-v0.9.0` 指向命名改造后的 master 并推两远端,客户端 pin 从破例 SHA 换回 tag ⇒ 破例期结束(`00` §9.0.25) |
| **23** | ✅ ~~⬜ ★★ **战场态宠物的战斗三围(attack/defense/quick/max_hp)恒为 0**~~ ⇒ **2026-09-09 批次 M.4b 关闭** | `00` §9.0.27 ③。`enterPetToField` 把 `Model::Pet` 投影成战场 `Combatant` 时三围**留 0**:`Pet` 世界态只有原始四维(vital/str/tough/dex)+ 成长率,而 attack/defense/quick 与 max_hp 是 `CHAR_complianceParameter` 从原始属性 + 等级 + 装备**推导**的,属性推导公式未移植(成长养成域 `06`)。⚠️ 后果:入场的宠物能被打、会死、能进出场,**但打不出伤害、极易被秒** —— 登记在案的残缺,不是 bug(同欠债 20 里捕获宠物四维=0、DR-BT19)。★ 佐证不是宠物独有:`makeDemoField` 里**玩家三围也是硬编码**的(`World.cpp:497`)⇒ `complianceParameter` 是被捕获 / 换宠入场 / demo 玩家三处**共同依赖的前置**,补它才是让宠物真正参战的那一步。⚠️ 别在零三围上接成长 / 战力计算(会把 0 当真值,同 Y 五项不建的理由)。<br>⚠️ **DR-BT21(换宠,2026-09-07,`00` §9.0.28 ⑥)未解本条,反而坐实了受影响面**:玩家主动 `PET_OUT` 叫出的宠走同一个 `enterPetToField` ⇒ 同样三围 0,"叫得出、但打不动"。⇒ `complianceParameter` 是**捕获 / 换宠入场 / demo 玩家**三处共同前置,是"让宠物真正参战"绕不开的下一步(成长养成域 `06`)。<br>🔧 **2026-09-08 批次 M.3 关上半(DR-DT9,`00` §9.0.29)**:`complianceParameter` 的基础段已移植为 `shared/rules/Progression.h::deriveBaseStats`,三处接线中的两处已接 —— `enterPetToField`(宠物入场据四维推三围)与 **`makeDemoField`(demo 玩家 / 敌人三围不再硬编码)**。⇒ **「推导缺失」这一半不再成立**。<br>⬜ ★★ **仍未做的下半 = 四维本身没有非 0 来源**,而它才是"打不动"的现存原因:捕获宠四维 = 0(DR-BT19,战场上的敌人只是 `Rules::Combatant`,没有四维可拷)· 一般宠创建(`PET_createPet` 从 `enemyindex` 拷四维)未移植 · 敌人模板属 L4 内容导入。⚠️★ **0 是推导的不动点** ⇒ 接了公式,宠物三围**照旧全 0**,现象与之前一字不差 —— 别把「上半已关」读成「宠物能打了」;`world_tick` 有一条用例专门钉住这件事。⇒ 真正让宠物参战的那一批 = **四维来源**(成长率消费 `PARAM_CAL` / 敌人模板导入),`06` §3.1 是它的入口。<br>★ 顺带的证伪见 `11` DR-DT9:旧的 demo 硬编码三围(`attack=300 / max_hp=400`)在原版公式下**不可达**,是一组自相矛盾的值 ⇒ 接线必然改变 demo 数值,而它要守的两条性质由用例复验仍成立。<br>🔧 **2026-09-08 批次 M.4a 补上下半的公式(DR-DT10,`00` §9.0.31)**:`PARAM_CAL` 已移植为 `shared/rules/Progression.h::rollSpawnStats`(模板 + 等级 → 四维 + 成长率),`06` §3.5 是文档入口。⚠️★ **但本条仍不关闭** —— 公式有了、**接线没有**:战场上的敌人依旧只是 `Rules::Combatant`,**没有 L2 实体可供捕获时拷四维** ⇒ 捕获宠四维照旧 0。⇒ 剩下的是 **M.4b:`EntityKind::kEnemy` 族 + 刷怪据模板生成 + 捕获路径改从敌人实体拷**,见欠债 25。<br>✅✅ **2026-09-09 批次 M.4b 关闭本条**(`00` §9.0.33 · `11` DR-BT22):`shared/model/Enemy.h` 建起 kEnemy 族 + `EntityPool<Enemy,10000>` 挂 `World` + `World::spawnEnemyToField` 据模板调 `rollSpawnStats` + 捕获改从**敌人 L2 实体**拷四维 / 成长率 / 名字 / 评级 / 图号 ⇒ **捕获出的宠物四维非 0**,叫出后战场三围也非 0。★ **关闭判据是可断言的**:`world_tick` 用 `playerPetAt()` 逐字段比对宠物四维与敌人实体那一份,并有一条端到端用例断言「叫得出、而且打得动」(`attack > 0 && max_hp > 0`)—— M.1–M.4a 期间这些断言必然全红。反向验证「捕获不拷四维」当场转红。<br>⚠️★ **两处残留,都不属本条**:① **一般宠创建**(`PET_createPet`,非战斗获得的宠物)仍未移植 ⇒ 经那条路来的宠物四维照旧 0(用例专门保留一条钉住它)—— 别把「捕获宠能打了」读成「所有宠物都能打了」;② demo 仍不接真实模板(量级 16 倍,M.4a 的结论 + 本批「满血怪抓不到」两条理由)。<br>★ 连带:`Model::Pet` 的 **Y 五项**(`YHP`/`YATK`/`YDEF`/`YQUICK`/`YLV`)本批**建起来了** —— M.1/M.2/M.3 三批「有意不建」的理由(四维无源 ⇒ 算出来只能是 0)随本条关闭而解除。⚠️★ 顺带纠正一句转述:那三批都写着 Y 五项是「升级 / 成长的基线」,而**源码里没有任何计算读它们** —— 全树实测写点 4 处、读点只有存档键表 + 两处下发显示串 ⇒ 它属**显示 + 存档**面,将来接成长时**不要**去读它。<br>✅ **2026-09-09 批次 M.5 兑现本条关闭时留下的两个入参**(`00` §9.0.35 · `11` DR-DT12):敌人表 `enemy1.txt` 已建为 `world/Api.h::EnemyEncounter` ⇒ **等级不再由调用方硬给**(据 `LV_MIN/LV_MAX` 摇号,`rollEncounterLevel`)、**`capturable` 归位敌人表**(从 `EnemyTemplate` 移出 —— 它从来属敌人表 c14 `ENEMY_PETFLG`,而模板表 c38 那个同名的 `E_T_PETFLG` **源码一处都不读**,是同名不同源的第五例)。⚠️★ **但"等级是入参"那一支没有被推翻,它就是原版** —— 源码 `:1030-1035` 是 `baselevel > 0 ? baselevel : RAND(...)`,NPC 触发的战斗走前者、野外遇敌走后者(分流靠 `battle.c:2178` 的局部初值 0,只有 `:2225` 那支赋值)⇒ 两支都实现,别把它读成"旧接口是权宜"。<br>⬜ **残留 ① 未变**:一般宠创建 `PET_createPet` 仍未移植 ⇒ 经那条路来的宠物四维照旧 0(用例专门保留一条钉住它)。|
| **25** | ✅ ~~⬜ ★★ **`rollSpawnStats` 已落地但未接线到任何世界写路径**~~ ⇒ **2026-09-09 批次 M.4b 关闭** | `00` §9.0.31。四维生成公式 + 10 例 / 68 断言的黄金用例集已在 `shared/rules`,⚠️ 但**没有任何调用方** —— `applyEvents` 的捕获路径仍给宠物填 0,`makeDemoField` 仍用手填四维。<br>★ 与**欠债 20** 完全同族(「L2 实体池已落地但未接线到 `World`」):**地基绿而运行时不接,`ctest` 一样 14/14 全过**。⇒ 这条不能靠"迟早会接"消掉,必须有观察面(欠债 20 关闭时的三笔:`petCount` / `playerPetSlotsUsed` / `playerCaptureCount` 就是为此加的)。<br>⚠️★ **demo 有意不接,且这不是偷懒 —— 是用例算出来的结论**:真实模板在低等级下产出的战力比 demo 手填的单位弱**约 16 倍**(乌力 18 级 ⇒ `attack 17 / max_hp 130`,demo foe 是 `attack 273 / max_hp 590`)⇒ 直接换成真实模板,敌人几乎打不动 860 血的玩家,`00` §9.0.16 ② 要守的 demo 性质①「无指令也有限回合结束」当场破。★ 用例 `四维生成:真实模板 18 级的战力量级` 把这个倍数关系**断言**下来,不只写在注释里。<br>⇒ **M.4b 的内容**:① `EntityKind::kEnemy` 族 + `EntityPool<Enemy>` 挂 World;② 敌人入场时据模板 + 等级调 `rollSpawnStats`;③ 捕获路径从敌人 L2 实体拷四维(`PET_createPetFromCharaIndex`,`char/pet.c:325-405`)⇒ 关闭欠债 23 与本条;④ `Model::Pet` 文末 ② 的 **Y 五项**(`YHP`/`YATK`/`YDEF`/`YQUICK`/`YLV`)届时才能建 —— 它们是初值快照,四维有源之后才有意义。⑤ 模板数据的来源仍是 L4 内容导入(D 线,`11` §13 记「无决策阻塞,可立即开工」;`enemybase1.txt` 数值列是纯 ASCII,不被 `04` §7.2 的混编链卡住)。<br>✅✅ **2026-09-09 关闭:①②③④ 全部落地**(`00` §9.0.33)。★ **关闭判据 = 观察面**,而不只是「有调用方」:`enemyCount()`(实体进没进池)· `battleEnemyAt()`(槽→实体映射建没建)· `playerPetAt()`(拷过来的四维是多少)—— 立案原话「地基绿而运行时不接,ctest 一样全过」⇒ **不可断言的关闭等于没关闭**。`world_tick` 40 → **55 例 / 890 断言**,含泄漏探针三条(捕获后回落 · 战斗结束归零 · 入场失败不留孤儿)。<br>⬜ **⑤ 仍未做,且是有判据地不做**:D 线的终点是**入库**(`04` §7.1)⇒ 做一个运行时读 `enemybase1.txt` 的加载器是**将来要删的第二次改动**。★ 本批的接线面不需要真文件就能完整验证(用例用第 1 行「乌力」的**全部实测列**做 fixture,名字列按 **GBK** 解 —— 实测不是 Big5)。⇒ 文件导入仍归 D 线,不算本条残留。<br>⚠️★ **本批新登记的两处偏差**(都不足以单开欠债,记在此处):① **敌人一律空手** —— `ENEMY_STYLE` 给敌人发武器(`enemy.c:1132-1150`)未移植 ⇒ 影响 `unarmed`/`weapon` ⇒ **比接了武器的原版更容易触发空手多段**(§3.9 可达 10 段);② **敌人池容量取 `othercharnum = 10000`**,而原版第三段是**敌人与 NPC 共用** ⇒ 建 NPC 族那一批**必须按族切分这 10000 的预算,不是各取 10000**(代码里已记,那时会读到)。<br>⚠️★★ **2026-09-09 批次 M.5 把上面偏差 ① 的规模改小了一个数量级(口径更正,偏差本身仍在)**:实测 `ENEMY_STYLE`(敌人表 c13)在 **2154 行里 2112 行是 0**(98%),而源码 `switch(0)` 落 `default` ⇒ `wepon` 保持 −1 ⇒ **不发武器** ⇒ **原版 98% 的敌人本来就是空手的** ⇒ 「我们的敌人一律空手 ⇒ 比原版更易触发空手多段」**仍然成立,但只影响 42 行(2%)的配置**,不是全部敌人。★ **教训:登记偏差也要在拿到数据后复核规模** —— M.4b 写它时手上没有敌人表数据。<br>⚠️★ **同时更正一处文件名口径**:本条与欠债 23、`00` §9.0.33/34 一律把敌人表写作 `enemy.txt`,而实测 `setup.cf` 的 `enemyfile` 指向 **`enemy1.txt`(34 列 / 2154 行)** —— `enemy.txt`(33 列 / 1494 行)是宏关闭时代的旧数据,两者 int 列**整体错位一位而每列都还是合法整数**(判据与踩坑过程见 `00` §9.0.35 ①)。⇒ 此后一律写 `enemy1.txt`;历史节保留原文。|
| **24** | ✅ ~~⬜ ★★ **`.clang-format` 纪律没有任何自动守卫 —— 且已经漂了**(新增 2026-09-08,批次 M.3)~~ ⇒ **2026-09-08 当日关闭**(`00` §9.0.30) | **原文**:P6(`00` §9.0.23)立下「五仓字节一致、CI `--Werror` 强制」,⚠️ **但 `ci_verify.py` 六项里没有格式项,`.github/workflows/` 也不跑 `clang-format`** ⇒ 那条「强制」从来没有执行者。实测(2026-09-08,批次 M.3 顺手核出):`HEAD` 上 `tests/WorldTickTest.cpp` 3 处、`src/world/World.cpp` 1 处违规(行尾注释对齐 + 一处折行),都是 P6 之后的 M.1 / M.2 / DR-BT21 三批带进来的。⚠️ 后果不是编译错也不是行为错,而是**「格式统一」这个已宣告完成的状态在静默退化**,且退化速度 = 每批一点。★ 与欠债 20 那条「地基绿而运行时不接,ctest 一样全过」同族:**立了纪律但没有任何东西会失败**。<br>✅ **处置**(`00` §9.0.30):`tools/check_format.py` + `ctest` 第 14 项 `code_format` + `ci_verify.py` 的 `EXPECTED_TESTS` + CI 三平台 `pip install clang-format==21.1.8`(钉版本)。⚠️★ **原处置候选记的「既有 4 处」偏低 —— 全仓实测是 10 处 / 5 个文件**(多出 `ModelPoolTest.cpp` 4 · `EntityPool.h` 1 · `RulesBattleTest.cpp` 1):那个数是按**批次改动面**数的,不是按全仓数的 ⇒ **没有全仓扫描就没有全仓结论**,这正是本条要消灭的东西。已由单独一笔 `style:`(`c31ae13`)修净,与守卫分两笔。★ 排除面与原候选一致(`idl/generated/` + `idl/codegen/`,判据:格式化生成物会让 `idl_verify` 逐字节比对转红 ⇒ **两道守卫互相打架**)——⚠️ 这是**显式登记的残留不是已覆盖**,正解是让 codegen 自己产出合规代码(改 `cpp.py`),见 `00` §9.0.30 ④。<br>⚠️★★ **2026-09-09 补记一处本条落地时留下的真缺陷**(`00` §9.0.33 ⑥):`check_format.py` 的 `DIAG_RE` 里那个 `THIS-WORDING-NO-LONGER-MATCHES` 是**做反向验证时故意换上的哨兵**,用完**忘了改回来、并连它一起提交**(`b6c718c`)⇒ 凡真有违规时退出码非 0 而解析到 0 条,**一致性断言抢先触发**,打印的是「判据自相矛盾」而不是违规清单。★★ **两天没被发现的原因比缺陷本身值钱**:那条一致性断言在**零违规**状态下两边同向(退出码 0 + 解析 0 条)⇒ 检查照常绿,而本条关闭当天的复验恰恰是在零违规状态下做的(违规已由前一笔 `c31ae13` 修净)⇒ **只有「该红的时候」才会暴露的缺陷,绿色证明不了它**。⇒ 已恢复真实措辞 `code should be clang-formatted` 并**两个状态都跑过**(先造违规确认打印出清单、再修掉)。★ 立下第三条纪律:**反向验证的改动必须与恢复动作成对,而且恢复后要再跑一次「该红」的场景。**|
| **26** | 🔧 ★★ **DR-BT23 改了 `shared/rules/` ⇒ 锁定 ref 须前推,且客户端侧 2,276 断言尚未验证**(新增 2026-09-09,批次 R.1)| `00` §9.0.36 ⑦。⚠️ **与 M.5 正好相反**:M.5 是「窗口内有改动但不在 watched 路径 ⇒ 不构成前推理由」,本批**真的改了 `shared/rules/RandomSource.h`** ⇒ watched 路径有实质改动 ⇒ 按 §9.0.34 ① 的判据**必须**打 tag 并前推客户端 `SA_SHARED_GIT_TAG`。<br>⚠️★★ **而真正的风险不在前推动作,在客户端那套断言**:客户端仓有自己的黄金用例集,它编译**同一份** `shared/` ⇒ 本仓 3,300+ 断言全绿**不能替它作证**(与 §9.0.15 / §9.0.17 ② 那条「联调态绿不能替发布态作证」同族,而这次换成了「本仓绿不能替对仓作证」)。★ 判据是明确的:客户端用例集若也只走它自己的脚本源,那边同样**不会红**;若它有任何一处直接用 `SeededRandom` 并断言逐值,**那里会红,而且那是它该红** —— ★ 与 `ci_verify.py` 的 `EXPECTED_TESTS` 在 DR-TS7 改名时失败同一条道理:**不要为了让它绿而放宽它**。<br>🔧 **联调态已验(2026-09-09 当日)**:客户端 `d2-only` **5/5**,`sa_client_rules_battle_test` **76 用例 / 2,415 断言全过**,★ 且 `-tc="*DR-BT23*"` 实测 **12 断言在客户端侧也编译并通过** ⇒ **「客户端会不会红」这个核心风险已排除**;★ 顺带坐实一件事:客户端的 golden test 接入能吃下本批新增的 `tests/support/` 子目录(否则构建当场失败)。⚠️★ **顺手更正一个数**:本条初稿写「客户端 2,276 断言」——那是 **v0.4.0 时期(§9.0.17 ②)的历史数字**,实测当前是 **2,415**(含本批 12 条)⇒ ★ 同 §9.0.35 ⑦ 那一族:**引用断言数/行数这类计数前要实测,别从历史记录里抄** —— 那个数在本项目已经记错过三次。<br>✅ **已闭合(2026-09-09,§9.0.38 窗口)**:`shared-v0.14.0` 早已双远端推送 + client 曾 pin 到它 —— 本批推 `shared-v0.15.0` 时 `ls-remote` 核实远端确有 `v0.14.0`(且 client 已从它前推到 `v0.15.0`)⇒ 该窗口早已闭合,本条状态此前**滞后**,随本批一并就地更正(§9.0.32 ⑤ 先例)。|
| **27** | 🔧 ★★ **战果结算改了 `shared/model` + `idl/generated` ⇒ 锁定 ref 须前推 `shared-v0.15.0`;升级 / 金钱 / 掉落 / 决斗点分配 / 客户端消费为登记残缺**(新增 2026-09-09,批次 战果结算)| `00` §9.0.40。<br>✅ **推送窗口已闭合(2026-09-09,与欠债 26 合并到 `shared-v0.15.0`)**:① server `004bbe0` + tag `shared-v0.15.0` / client `fc846f9` 双远端(gitee+github)`ls-remote` 核实一致、tag 集合差为空(没顺带别的);② **server CI #31 三平台 success** · **client CI #21 三平台 success 且发布态 `fetch` 锁定 ref = `shared-v0.15.0`**(76 用例/2415 断言)—— ★ 发布态那半是本地联调态覆盖不到的,本批兑现。<br>⬜ **登记残缺(有据划出,见 `11` §2.17 ⑤ —— 不是本批做不了,是不该在本批做)**:① **升级**(走 `exp.txt`,D 线未导入 + 成长域)⇒ 经验只累积不升级,⚠️ **不撞** `fmdplevelexp`(那是家族声望);② **金钱**(经济域 `GoldLedger`,阶段 2.2 单入口);③ **掉落**(道具域,与 `USE_ITEM` 同批);④ **决斗点分配**(PvP / saac 域;本批只建敌人 `duelpoint` 初值 + `dpbattle` 判定,决斗点怪打赢**暂不给点**);⑤ **骑宠经验**(源码 `× 0.6`;`Model::Pet` 无 `exp` 字段 + 属宠物成长域);⑥ **客户端消费 `BattleResult`**(schema 已建、服务端已发,`BattleScene` 演出留客户端批次)。|
| **28** | 🔧 ★★ **世界敌人 W.2+W.3 改了 `idl/generated`(视野加 `entity_type`/`image`)+ `shared/model`(Enemy 加位置)⇒ 锁定 ref 须前推;明雷触发战斗 / 完整 AI / 时间预算截断为登记残缺**(新增 2026-09-10,批次 W.2+W.3)| `00` §9.0.45 · `11` DR-DT18。<br>⚠️★★ **两处 watched 变更**(`world_map.proto` 加 `entity_type`/`image` · `Enemy.h` 加 floor/x/y/dir + `next_wander_at_ms`)⇒ 与 W.1 同,须打 tag 前推 `shared` + 客户端换 pin 验发布态(推送窗口待办)。<br>⬜ **登记残缺**:① **明雷触发战斗** —— 玩家撞上世界敌人开战(把世界敌人拉进战斗那一层),解 W.4「明雷退回」;当前世界敌人可见、会游荡,但撞上**不开战**;② **完整 AI**(`CHAR_LOOPFUNC` 追击 / 攻击玩家 · `RunCharLoopEvent` Lua)⇒ 依赖脚本层 D6 + 函数指针数据驱动化;③ **时间预算截断路径**(`_CHAR_LOOP_TIME`,8.0 三证关 ⇒ 不实现;墙钟不可确定性复现,结构留 `charloop_cursor` 但不做确定性用例);④ **`_FIX_CHAR_LOOP` 的 other 段上限 50 未核**(fixture 规模无差异;敌人规模大且需精确摊还时回 B80 核);⑤ 真实刷怪点 / 多 floor ⇒ D 线 / D6。<br>⚠️★ **摊还的取证教训(记账,非残缺)**:`unifdef_80` 展开视图把 `_CHAR_LOOP_TIME` 当"开"、展开成时间预算 while,而 8.0 实测关 ⇒ 本批**先选错分支**差点照做,由 `15 §5.2 C18` 三证纠正(编译期 `-D` 宏 unifdef 靠 `macros_80.json` 猜开关会猜错,见 `00` §9.0.45 ①)。|
| **29** | 🔧 ★★ **明雷触发战斗 W.5 改了 `idl/generated`(加 EV 两消息)⇒ 锁定 ref 须前推 `shared-v0.18.0`;NPC 脚本门 / 掉落 / 决斗点场 / 通用事件表为登记残缺**(新增 2026-09-10,批次 W.5)| `00` §9.0.46 · `11` DR-DT19。<br>⚠️★★ **watched 变更**:`world_map.proto` 加 `EventRequest`(0x0305)/`EventResult`(0x0306)⇒ `idl/generated` 重生成 ⇒ 与 W.1/W.2+W.3 同,须打 tag 前推 `shared-v0.18.0` + 客户端换 pin 验发布态(推送窗口待办)。<br>✅ **本批兑现欠债 28 ①「明雷触发战斗」**:撞明雷退回(`char_walk.c:585`)+ EV 事件开战(`EVENT_main` → `NPC_NPCEnemy_BattleIn`)两者**从零做**(⚠️ 勘察实证退回从未实现,明雷交互整个是缺口,非"改退回为开战");明雷开战复用**已存在**敌人实体(转移 handle 所有权、`enemyCount` 守恒,不 spawn 新的)。<br>⬜ **登记残缺**:① NPC `argstr` 脚本门(gym/item/startmsg/steal/deniedmsg ⇒ D6 脚本层);② 胜利掉落(`NPC_NPCEnemy_Dying` ⇒ 道具域);③ `gym`→mode 2 决斗点场(PvP/saac);④ 通用事件表(传送点 `_MAP_WARPPOINT` 等,`EVENT_main` 骨架已留、本批只接明雷一路);⑤ 精确复活时机(`DIETIME`/`REVIVALTIME`,当前用 count 立即补齐替代);⑥ 客户端 EV 实装 + 坐标纠正 XYD(客户端仓 / W.1 走路同步残缺)。|
| **30** | 🔧 ★★ **背包 L2 地基 I.1 改了 `shared/model`(新 `Item.h` + `Player.h` 加 `items[]`)⇒ 锁定 ref 须前推 `shared-v0.19.0`;道具池写入链路(扣/掉/用)为登记残缺**(新增 2026-09-10,批次 I.1)| `00` §9.0.48 · `11` DR-DT20。<br>⚠️★★ **watched 变更**:新增 `shared/model/Item.h` + `Player.h` 加背包槽 ⇒ 与 W.1/W.2+W.3/W.5 同,须打 tag 前推 `shared-v0.19.0` + 客户端换 pin 验发布态(含 server `234d4cc`(§9.0.47 清账,ahead 1)一起推)。<br>✅ **推送窗口已闭合**(2026-09-10,`00` §9.0.49):server `655c02d`+tag / client pin `62a1ff1`,两仓×两远端 SHA/tag 一致、tag 集合差空,server CI #36 + client CI #25 三平台全 success + 发布态 fetch 日志核实锁定 ref=v0.19.0。<br>✅ **本批兑现道具域三条链的共同前置**:Item 族 POD + `EntityPool<Item,10000>` 挂 `World` + `Player.items[54]`(装备位 9 + 背包 45 连续布局)+ `findFreeItemSlot`/`clearItemSlot` + 观察面 `itemCount`/`playerItemSlotsUsed`(本批恒 0,用例断言之)。⇒ 捕获第 5 步 `BATTLE_CaptureItemDelAll`(DR-BT10「全删」)的落脚点解锁。<br>⬜ **登记残缺(道具域后三批)**:① **捕获扣道具**(补 §9.0.26「原版扣了道具才给宠物、现在白给」;需 `needitemeneny.txt` 敌人侧表);② **掉落**(`NPC_NPCEnemy_Dying` → `ITEM_makeItemAndRegist` 写背包);③ **使用道具**(`lssproto_ID_recv` → `CHAR_ItemUse`,效果族在无 Lua functable 下的落地形状待定);④ **当前堆叠数** `current_pile`(运行期状态,写入侧建);⑤ 装备加成 / 合成 / 镶嵌 / 魔法道具 / 状态附加(各属其域,`Item.h` 文末逐条登记);⑥ **地面道具**(掉在地上的,与背包道具是否共用 `Item` 结构留掉落批定,共用 10000 池)。<br>⚠️★ **取证教训(记账)**:连带抓到 `unifdef_80` **第二次宏误判**(`_ALLBLUES_LUA_1_8` 判开、StoneAge 全树关 ⇒ 以 StoneAge 为准,继 W.3 `_CHAR_LOOP_TIME` 后 evidence-workflow 第二强样本 —— 争议处必回 StoneAge/stoneage85 双核)。|
| **31** | 🔧 ★★ **捕获扣道具 I.2 改了 `shared/model/Enemy.h`(加 `pet_id`)+ 新增 `shared/rules/CaptureItem.h` ⇒ 锁定 ref 须前推 `shared-v0.20.0`;删后属性重算 / 道具入包写入链路为登记残缺**(新增 2026-09-10,批次 I.2)| `00` §9.0.50 · `11` DR-DT21。<br>⚠️★★ **watched 变更**:`Enemy.h` 加 `pet_id`(捕获匹配键)+ 新增 `shared/rules/CaptureItem.h`(需求表 + 匹配)⇒ 与 W.1/W.5/I.1 同,须打 tag 前推 `shared-v0.20.0` + 客户端换 pin 验发布态(与 I.3 合窗口;含 server `81075ee`(§9.0.49 记录,ahead 1)一起推)。<br>✅ **推送窗口已闭合**(2026-09-10,`00` §9.0.52):server `6a8384f`+tag / client pin `9fcb992`,两仓×两远端 SHA/tag 一致、tag 集合差空(各 20),server CI #37 + client CI #26 全 success + 发布态 fetch 日志核实锁定 ref=v0.20.0。<br>✅ **本批兑现欠债 30 ①「捕获扣道具」**:补 §9.0.26「原版扣了道具才给宠物、现在白给」缺口 —— 前置门 `CaptureItemCheck`(攻方无所需道具则捕获失败、不摇 rng,投影到 `capture_item_ok`)+ 捕获成功后 `CaptureItemDelAll` 全删(第 5 步);`itemCount` 从「恒 0」变「能减」。⚠️ **更正**:需求表 `_NEED_ITEM_ENEMY` 8.0 **关** ⇒ 净核**不读 `needitemeneny.txt` 文件**,用硬编码 `NeedEnemy[9]`(欠债 30 ①「需 needitemeneny.txt」与 DR-DT20 ⑥ 均据此更正)。<br>⬜ **登记残缺**:① `CHAR_complianceParameter`(删道具后属性重算,装备加成未移植 ⇒ 无可观察后果,属装备域);② 道具入背包**写入链路**(掉落/捡起,道具域后两批;当前只有 `giveItemToPlayer` 注入 seam);③ `needitemeneny.txt` 文件加载器(`_NEED_ITEM_ENEMY` 8.0 关,净核不需要)。<br>⬜ **道具域剩两批**:掉落(`NPC_NPCEnemy_Dying`)· 使用道具(`lssproto_ID_recv`→`CHAR_ItemUse`)。|
| **32** | 🔧 ★★ **使用道具 I.4 改了 `shared/model/Item.h`(加 `current_pile`)+ `shared/rules/{Battle.cpp,Combatant.h}` ⇒ 锁定 ref 须前推 `shared-v0.21.0`;其余 usefunc 族 / 场景内使用 / 目标已死改打为登记残缺**(新增 2026-09-11,批次 I.4)| `00` §9.0.53 · `11` DR-DT23。<br>⚠️★★ **watched 变更**:`Item.h` 加 `current_pile`(运行期堆叠数)+ `Combatant.h` 加 `mods.item_heal_power` + `Battle.cpp` 新增 USE_ITEM 分支 ⇒ 与 W.1/W.5/I.1/I.2 同,须打 tag 前推 `shared-v0.21.0` + 客户端换 pin 验发布态(含 server `6e57229`(§9.0.52 推送记录,ahead 1)一起推)。<br>✅ **推送窗口已闭合**(2026-09-11,`00` §9.0.54):server `1708cab`+tag / client pin `d89f675`,两仓×两远端 SHA/tag 一致、tag 集合差空(各 21),server CI #38 三平台 success(**I.4 的 MSVC 首验**)+ client CI #27 三平台 success + 发布态 fetch 日志核实锁定 ref=v0.21.0 与源码一致;★ **84 例/2493 断言在本地 server / 本地联调态 / CI 发布态三处逐位一致**(本批改变了 rng 消耗,这是该类改动唯一能拿到的正确性证据)。<br>✅ **本批兑现欠债 30 ①「用」这条链**(扣/掉/用三链至此全通,道具域收官):战斗内 HP 恢复药 —— `ItemEffect` 表 + `projectItemUsePower`(resolveTurn 前投基数)+ L3 摇 `RAND(0.9p,1.1p)` + `consumeUsedItems`(扣 pile、清槽+release 成对)。<br>⚠️★★ **本批订正了一处已落地实现的正确性错误**(不是新欠债,是已修):工作树里原实现把恢复量当确定值、不摇 rng,而源码 `battle_magic.c:419` 是 `RAND(power*0.9, power*1.1)` ⇒ 会让**用过道具后的 rng 序列整体平移**(DR-BT23 同族,返回值断言抓不到)。已按源码更正并以 `calls()` 断言 + 反向验证 A 钉住。<br>⏳ **登记残缺**:① MP 恢复 / 状态药 / 变身 / 传送 / 解猪 / 属性旋转等其余 usefunc(各依赖未移植子系统)· ② 场景内使用 `ITEM_useRecovery_Field`(需场景态 HP 与提示链)· ③ **目标已死时原版「随机改打活人」不复刻** (`BATTLE_MultiList` `battle.c:236`,消耗不定次 rng + 全死时原版 UB)⇒ 已知行为差 · ④ `ITEM_TYPE` 非 OTHER/DISH 的穿装备门(装备域)· ⑤ `power==-1` 全满恢复(需道具表真数据)· ⑥ `CanCureFlg` 不可治疗门(L4 状态系统)· ⑦ 道具效果表真数据(D 线 `itemset6.txt`)· ⑧ 客户端表现(用药动画/飘字)。 <br>**2026-09-12 当前更正**：F07 改为实际执行后扣药；有活目标时恢复原版死亡目标改选和 RNG 消耗，旧残缺③仅剩整侧全死的 UB 安全跳过。浮点 RAND 分布已核对。历史“resolveTurn 前统一投影/扣药”的描述由逐行动提交取代；其余功能覆盖缺口继续保留。 |
| **33** | 🔧 ★★ **状态异常 L4.1 改了 `shared/rules/`(新增 `Status.{h,cpp}` + `Combatant.h` 加四维/抗性)+ `idl/generated`(新增 `StatusTick` 事件)⇒ 锁定 ref 须前推 `shared-v0.22.0`;状态 12..43 / 职业施加路径 / 酒醉解除骑宠分支为登记残缺**(新增 2026-09-11,批次 L4.1)| `00` §9.0.55 · `11` DR-DT24。<br>⚠️★★ **watched 变更**:新增 `shared/rules/Status.{h,cpp}` + `Combatant.h` 加原始四维 `vital/str/tough/dex` 与骑宠四维 `ride_*` + 六类抗性字段 + `Battle.cpp` 接入状态推进 + `world_map` 无关但 `battle_events.proto` 新增 `StatusTick`(oneof 26)⇒ `idl/generated` 重生成 ⇒ 与 W.1/W.5/I.1/I.2/I.4 同,须打 tag 前推 **`shared-v0.22.0`** + 客户端换 pin 验发布态(含 server `1f9ac54`(§9.0.54 推送记录,ahead 1)一起推)。<br>✅ **本批兑现「状态系统零写入者」这条静默**:`Combatant::status` / `status_turns` 与 `checkCanAct` 的 8 项否决自阶段 1 就在,但**实测全仓只有 tests 在写 `status`、`status_turns` 连读点都没有** ⇒ 标准的欠债 20 族。本批建起施加(带毒装备,`_SUIT_ADDPART4`)→ 每回合推进(`BATTLE_StatusSeq`)→ 解除三段链路,`status` 第一次有运行时写入者。<br>⚠️★★ **最该记的一条:`StatusTick` 事件是被 `world_tick` 用例逼出来的,而缺它时 `rules_battle` 102 例全绿** —— 初版只在解除时发 `StatusChange`,递减只活在 L3 的回合内镜像里,**没有任何事件把它带回世界** ⇒ 世界态 `status_turns` 永远停在施加值,直到某回合突然消失。★ 也**不能让世界侧自己减一**:§4.2 的虚弱/魔障会把递减**加回去**,世界侧自己算 = 把那条规则实现第二遍(DR-BT5),而分叉点恰是「虚弱/魔障永不自然解除」这条玩法级强约束。⇒ 同 A.4 `KnockbackState` 的形状:权威态回写,不是演出事件。<br>⚠️★★ **两处过程教训(记账,非残缺)**:① 抓到上面那个 bug 时我先归因成「敌人 AI 未移植 ⇒ 敌人永不推进」并据此加了一段「补跑无指令槽」的预推进 —— **那个前提是错的**(`World.cpp` 的 `fillEnemyCommands` 一直在),补跑只会在 L3 fixture 里触发,是凭空造出的行为偏差,已删除。★ **归因要落到实证**,一次 `grep` 就能证伪,而我是先写了修复、再由第二个失败用例把假前提暴露出来的。② **`make` 秒级 mtime 坑第三次发作**:反向验证 F 第一次跑出「不转红」,我据此在用例里写下「本条没有区分力」的注释;换**全新构建目录**重跑 F 精确转红 ⇒ ★★ **此后反向验证一律用全新构建目录**(`mktemp -d` + 重新 configure),`touch` + 跨秒只挡得住同秒,挡不住「目录已被上一次注入污染」。<br>⬜ **登记残缺**:① **状态 12..43**(晕眩/天罗/冰爆术族等 32 种)—— 属宠技/职技域;② **职业技能施加路径**(`PROFESSION_BATTLE_StatusAttackCheck`,能绕过全局互斥的**只有 11 种**,判据已记在 `Status.h`);③ **精灵/魔法两组调用参数**(`Range=30`/`Bai=1.0`,需技能表);④ **施加当场清目标指令**(麻痹/睡眠/石化/魔障四种)—— 本批唯一施加者产出的是**毒**、不在那四种里 ⇒ **无输入能执行** ⇒ 只建判据函数 `clearsCommandOnApply` + 单元用例,不写不可反验的死代码;⑤ ⚠️★★ **酒醉解除的骑宠分支**(`quick += 骑宠 quick`,`battle.c:5492`)—— `Combatant` 无 `ride_quick`,且 **`has_ride` 在世界侧从未被写入过**(骑乘系统未移植)⇒ 运行时不可达 ⇒ 本批只实现无骑宠那一支(`× 2`)。★ **它是会静默引爆的雷**:骑乘系统接上后 `has_ride` 变真,这里会**照旧走 ×2** 而不报任何错;⑥ `CanCureFlg` 不可治疗门(兑现 I.4 残缺 ⑥ 的前置,门本身需状态药路径);⑦ 客户端表现(中毒图标 / 掉血飘字)。<br>⚠️★ **另记一条排期改判(不属残缺,属依赖图更正)**:`00` §9.0.55 实证「反击排在 L4 之后」这个四份文档一致的前提**有一半不成立** —— `BATTLE_GetDamageReact`(`battle_event.c:1819`)读 5 个**独立 work 字段**、`BATTLE_CounterCheckPlayer`(`:3465`)**一个 status 槽都不读** ⇒ 反击可在 `react == NONE` 路径上独立落地。⇒ 用户 2026-09-11 裁定:记录在案,**L4 之后单开一批**,欠债 1 里「反击排在 L4 之后」那句的**理由**据此更正(不是被状态槽阻塞,是那 5 个字段的写入者未移植)。 <br>**2026-09-12 修复补记**：状态计数与酒醉/混乱行为统一，睡眠受伤即时唤醒，四维和临时 quick 来源补正；当前等待玩家输入，不能用无人输入 demo 代表状态计时。共享引用与本地/远端发布状态统一见[修复结果 §7](../audits/2026-09-12-remediation-results.md#7-两仓共享引用)，不沿用本行历史“待前推”作为当前判断。 |

---
